export const idealAnswers = {
  // Python Developer
  'py-ez-1': "The primary distinction is mutability: lists are mutable (can be altered in-place), whereas tuples are immutable (cannot be changed after creation). Syntactically, lists use square brackets `[]` and tuples use parentheses `()`. In terms of memory, tuples are allocated as a single, contiguous block, making them slightly faster to instantiate and highly memory-efficient. You prefer a list for dynamic collection modifications (e.g., appending or sorting). You prefer a tuple for fixed records, read-only data integrity, and as dictionary keys (since they are hashable).",
  'py-ez-2': "List comprehensions provide a concise, readable, and highly performant way to create lists in Python. The syntax is `[expression for item in iterable if condition]`. Under the hood, list comprehensions run at C-speed inside the Python interpreter, avoiding the overhead of repeated `.append()` function lookups on a list object. For example, `squares = [x**2 for x in range(10)]` is cleaner and faster than initializing an empty list and appending values inside a traditional `for` loop.",
  'py-med-1': "A Python decorator is a higher-order function that takes another function, extends its behavior without modifying it explicitly, and returns a new wrapper function. It relies heavily on closures—a mechanism where an inner function retains access to variables in its outer lexical scope even after the outer function has completed execution. A standard use-case is logging or caching, where `@functools.wraps` is applied to preserve the original function's metadata.",
  'py-med-2': "`__new__` is the actual constructor method responsible for creating and returning a new instance of a class, taking `cls` as its first argument. `__init__` is the initializer method that configures the newly created instance, taking `self` as its first argument. `__new__` is overridden for advanced metaprogramming, creating custom classes, customizing immutable types, or implementing design patterns like the Singleton pattern.",
  'py-hrd-1': "The Global Interpreter Lock (GIL) is a mutex in CPython that ensures only one native OS thread executes Python bytecode at a time, preventing race conditions in memory reference counting. For CPU-bound tasks, multithreading is bottlenecked because threads block each other. To achieve true parallel execution on multi-core systems, developers must use the `multiprocessing` module (which spawns independent processes with separate memory spaces) or write C-extensions that release the GIL.",
  'py-hrd-2': "Python utilizes reference counting as its primary garbage collection mechanism. When an object's reference count drops to zero, its memory is instantly freed. To handle circular reference cycles (where objects reference each other, keeping counts > 0), Python runs a cyclic garbage collector using three generations. Objects that survive scans are promoted to older generations, which are scanned less frequently to optimize execution performance.",

  // Frontend Developer
  'fe-ez-1': "The Virtual DOM is a lightweight, in-memory JavaScript representation of the real DOM. When state changes, React constructs a new Virtual DOM tree, performs a 'reconciliation' process to compare it with the previous tree using a highly optimized O(N) diffing algorithm, and batches the minimum required updates. This avoids costly direct browser layout calculations and paint cycles, drastically optimizing rendering speed under the hood.",
  'fe-ez-2': "State represents local, mutable data owned and managed entirely within a component, created via hooks like `useState`. Props are read-only, immutable configuration values passed down from parent to child components, establishing a strict unidirectional data flow. Props cannot be modified by the child to maintain predictable rendering behavior; instead, state updates in the parent can trigger new prop inputs downstream.",
  'fe-med-1': "React Hooks allow functional components to hook into state and lifecycle features previously exclusive to class components. They were introduced to promote code reusability, eliminate complex class bounds, and keep components flat. The two absolute rules are: 1) Only call hooks at the top level (never inside loops, conditions, or nested functions) to preserve the exact hook call order, and 2) Only call hooks from React functional components or custom hooks.",
  'fe-med-2': "JavaScript is single-threaded, using an Event Loop to manage asynchronous execution. Synchronous code executes immediately on the Call Stack. When an async API completes, its callback goes to either the Microtask Queue (Promises, `queueMicrotask`) or the Macrotask Queue (`setTimeout`, `setInterval`). Once the Call Stack is empty, the Event Loop prioritizes the entire Microtask Queue, flushing it completely before pulling a single task from the Macrotask Queue.",
  'fe-hrd-1': "Optimization of large React apps is achieved by: 1) Code-splitting using `React.lazy()` and `Suspense` to load bundles on-demand, 2) Memoization via `React.memo`, `useMemo`, and `useCallback` to prevent unnecessary child re-renders, 3) State localization to keep rendering boundaries narrow, and 4) Virtualization (`react-window`) to only render visible DOM nodes when working with massive lists.",
  'fe-hrd-2': "Server-Side Rendering (SSR) generates the HTML for each page on the server for every request, improving SEO and initial load. Static Site Generation (SSG) pre-compiles HTML at build-time. Hydration is the client-side process where React loads in the browser, scans the server-rendered HTML, and attaches event listeners to the DOM nodes, transforming the static page into a fully interactive React application.",

  // Backend Developer
  'be-ez-1': "A database index is a data structure (typically a B-Tree or Hash Index) that provides fast lookups for specific rows, bypassing slow full-table scans. The cost is write overhead: every insert, update, or delete transaction must update the index structures in addition to the table itself. Indexes also consume additional disk storage space, requiring careful planning to index only high-frequency filter and join columns.",
  'be-ez-2': "GET requests are designed to retrieve data, are idempotent, have payloads in the URL query string, and are heavily cached by browsers and CDN layers. POST requests are designed to create resources, are non-idempotent, carry payloads inside the HTTP request body (handling sensitive or massive data securely), and are never cached by default, maintaining transaction security.",
  'be-med-1': "Relational databases (SQL) enforce structured schemas, relational integrity, and guarantee strict ACID transactions, making them ideal for financial or transactional systems. Non-relational databases (NoSQL) offer dynamic schemas, horizontal scalability, and high write availability (leveraging document, key-value, or graph models). You select NoSQL for massive unstructured data streams, caching, or real-time scaling.",
  'be-med-2': "JSON Web Tokens (JWT) consist of three parts: Header, Payload, and Signature, base64-encoded and separated by dots. Safe authentication uses short-lived access tokens stored in-memory, paired with long-lived, HttpOnly, Secure, and SameSite refresh tokens stored in cookies. Refreshing is handled by an endpoint that validates the refresh token against database revoke lists and issues a new access token.",
  'be-hrd-1': "To handle 10k push events/sec, I would construct a decoupled microservice architecture: 1) An API gateway with rate-limiting, 2) Message queues (RabbitMQ/Kafka) to buffer event peaks, 3) Worker fleets that pull events, query connection registries, and 4) A real-time socket cluster (Node.js/Go WebSockets) integrated with Redis Pub/Sub to push notifications to active client connections instantly.",
  'be-hrd-2': "Database sharding is horizontal partitioning that distributes rows of a table across multiple database engines based on a shard key. Unlike replication (which copies identical data for read scalability), sharding splits the dataset for write and storage scaling. Key challenges include executing cross-shard joins, managing distributed transactions, re-balancing shards, and preventing hot-spotting.",

  // Data Structures & Algorithms
  'dsa-ez-1': "Breadth-First Search (BFS) explores nodes level-by-level using a FIFO Queue, making it ideal for finding the shortest path in unweighted graphs. Depth-First Search (DFS) explores as deep as possible along each branch before backtracking, implemented via recursion or a LIFO Stack. BFS space complexity peaks on wide graphs (storing frontier nodes), while DFS space complexity peaks on extremely deep paths (storing recursive frames).",
  'dsa-ez-2': "Big-O notation describes the asymptotic upper bound of an algorithm's run time or memory usage as the input size N grows. QuickSort has an average time complexity of O(N log N) but can degrade to O(N^2) in the worst-case (poor pivot choice), sorting in-place with O(log N) stack space. MergeSort guarantees O(N log N) time complexity but requires O(N) auxiliary space to merge divided arrays.",
  'dsa-med-1': "Dynamic Programming (DP) solves complex problems by breaking them into overlapping subproblems, leveraging optimal substructure. It differs from greedy algorithms which choose the local optimum at each step. Memoization (top-down) uses recursion, caching subproblem results as they are hit. Tabulation (bottom-up) builds an iterative table, filling it systematically to eliminate recursion stack overhead.",
  'dsa-med-2': "A Hash Map maps keys to values by passing keys through a hash function to calculate bucket array indexes. Hash collisions occur when two distinct keys yield the same index. They are resolved via: 1) Chaining, where each bucket holds a linked list/tree of colliding entries, or 2) Open Addressing (Linear/Quadratic Probing or Double Hashing), where the map searches for alternative vacant indexes in the array.",
  'dsa-hrd-1': "Dijkstra's algorithm finds the shortest path from a single source node to all other nodes in a weighted graph with non-negative edges. It uses a Min-Heap/Priority Queue to greedily select the unvisited node with the absolute smallest path estimate. The time complexity using a binary heap is O((E + V) log V), where E is the number of edges and V is the number of vertices, optimizing path discovery.",
  'dsa-hrd-2': "To detect cycles in a directed graph, we can perform Topological Sorting: 1) Kahn's Algorithm calculates the in-degree of all nodes, pushes 0-indegree nodes to a queue, processes neighbors, and checks if all nodes were visited (if count != V, a cycle exists). 2) DFS tracks nodes currently in the recursion stack; if a back-edge points to an active ancestor node on the stack, a cycle is detected.",

  // HR & Behavioral
  'hr-ez-1': "A successful elevator pitch should connect past professional achievements directly to the target role. Summarize your tech stack, highlights of a key project, and transition smoothly into why this company's mission aligns with your personal values. Conclude by specifying how your background can solve immediate operational challenges for the engineering team.",
  'hr-ez-2': "For strengths, highlight a professional soft skill (e.g., active problem-solving or cross-team collaboration) backed by a quick example. For weaknesses, state a genuine development area (e.g., struggling to delegate or getting caught in micro-optimization) and immediately outline the proactive steps, workflows, or frameworks you are using to actively manage and improve it.",
  'hr-med-1': "Begin by describing a professional disagreement (Situation), clarify the competing views without criticizing your colleague (Task), detail how you initiated a structured, data-driven discussion to compromise (Action), and finish with the positive project outcome, lessons learned, and the strong long-term relationship built with your peer (Result).",
  'hr-med-2': "Apply a structured prioritization framework like the Eisenhower Matrix (urgent vs. important) or Kanban boards. Describe communicating proactively with product managers to clarify specs, managing stakeholder expectations early when capacity limit is reached, breaking deliverables into smaller milestones, and optimizing your schedule by blocking out dedicated deep-work slots.",
  'hr-hrd-1': "Take absolute ownership of the project failure (Situation). Describe communicating the issue transparently to key stakeholders with an immediate mitigation plan (Action), and reflect on the key technical or structural lessons learned, illustrating how you implemented those changes in subsequent sprints to guarantee success (Result).",
  'hr-hrd-2': "Explain establishing trust by listening to team paint-points (Situation). Detail building consensus by compiling data-backed visual prototypes or benchmarks, aligning the initiative with company goals, and coordinating a collaborative workspace where everyone had input, ultimately delivering the project ahead of schedule with zero direct authority."
};
